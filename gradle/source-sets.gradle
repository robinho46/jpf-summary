sourceSets {
    main {
        java.srcDirs = ["src/main"]
        output.resourcesDir = file("${buildDir}/main") // Nyare sätt att ange output-dir
    }
    examples {
        java.srcDirs = ["src/examples"]
        output.resourcesDir = file("${buildDir}/examples")
        compileClasspath += sourceSets.main.output
    }
}
